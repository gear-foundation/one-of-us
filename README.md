# One of Us 🤝

Маркетинговая кампания для Vara.eth TestNet

**Цель:** 10,000+ адресов за 30 дней

---

## Функции

### `join_us()`

Присоединиться к нам

- `true` = добавлен
- `false` = уже с нами

### `count()`

Сколько нас

- Возвращает число

### `is_one_of_us(address)`

Проверка по адресу

- `true` = один из нас
- `false` = ещё нет

### `list(page, page_size)`

Получить список всех адресов

- `page` = номер страницы (с 0)
- `page_size` = размер страницы (например, 100)
- Возвращает список адресов

---

## Сборка

```bash
cargo build --release
cargo test
```

---

**Stop fragmenting. Start building together.**
